# Хеш-таблица (Hash Table)

## 📝 Описание
Хеш-таблица - это структура данных, реализующая интерфейс ассоциативного массива.

## 🎯 Основные операции
- `insert`: O(1) в среднем, O(n) в худшем случае
- `search`: O(1) в среднем, O(n) в худшем случае
- `delete`: O(1) в среднем, O(n) в худшем случае

## 📋 Интерфейс
```cpp
template <typename Key, typename Value>
class HashTable {
public:
    void insert(const Key& key, const Value& value);
    bool search(const Key& key, Value& value) const;
    void remove(const Key& key);
    bool contains(const Key& key) const;
    size_t size() const;
    bool empty() const;
    
private:
    size_t hash_function(const Key& key) const;
    void rehash();
};
```

## 📌 15 фундаментальных задач
1. Подсчет частоты элементов в массиве
2. Поиск пары с заданной суммой
3. Удаление дубликатов из массива
4. Проверка на анаграммы
5. Поиск подмассива с нулевой суммой
6. Нахождение первого неповторяющегося символа
7. Группировка анаграмм
8. Подсчет количества подмассивов с заданной суммой
9. Поиск симметричных пар в массиве
10. Проверка изоморфизма строк
11. Нахождение самой длинной подпоследовательности
12. Подсчет уникальных элементов в окне размера k
13. Проверка на подмассив с нулевой суммой
14. Нахождение всех троек с нулевой суммой
15. Реализация LRU кэша

## 🔗 Полезные ссылки
- [GeeksforGeeks - Hashing Data Structure](https://www.geeksforgeeks.org/hashing-data-structure/)
